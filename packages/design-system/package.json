{
  "name": "design-system",
  "version": "0.1.0",
  "private": true,
  "description": "Shared UI component library for all three products. Built with React, Tailwind CSS, and shadcn/ui. Documented via Storybook. Design tokens ensure visual consistency. UI prototypes created in Stitch (stitch.design) and validated through paywallexperiments.com for conversion feedback.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "dev": "storybook dev -p 6006",
    "storybook": "storybook dev -p 6006",
    "build-storybook": "storybook build",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "class-variance-authority": "^0.7.0",
    "clsx": "^2.1.0",
    "tailwind-merge": "^2.3.0",
    "lucide-react": "^0.400.0"
  },
  "devDependencies": {
    "@storybook/react-vite": "^8.2.0",
    "storybook": "^8.2.0",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}

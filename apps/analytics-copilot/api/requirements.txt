# Analytics Copilot API Dependencies
# Rationale: Powers the Text-to-SQL natural language query engine.
# Users type questions in plain English; the API translates them to SQL.
#
# Steps:
# 1. FastAPI + Uvicorn for the async query API
# 2. OpenAI SDK for GPT-powered natural language to SQL translation
# 3. SQLAlchemy for database connectivity and query execution
# 4. DuckDB for fast in-process analytical queries
# 5. python-dotenv for API key management
# 6. httpx for async external API calls
# 7. Cachetools for query result caching

fastapi==0.104.1
uvicorn[standard]==0.24.0
openai==1.6.1
sqlalchemy==2.0.23
duckdb==1.1.3
python-dotenv==1.0.0
httpx==0.25.2
cachetools==5.3.2
pytest==7.4.3
pytest-asyncio==0.23.2

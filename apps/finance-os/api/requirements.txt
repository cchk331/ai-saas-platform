# Finance OS API Dependencies
# Rationale: These packages power the FastAPI backend for cash flow
# forecasting, bank integrations, and financial data processing.
#
# Steps:
# 1. FastAPI + Uvicorn for async API server
# 2. Plaid SDK for bank account connectivity
# 3. Pandas + NumPy for cash flow analysis and forecasting
# 4. SQLAlchemy for database ORM
# 5. Pydantic for request/response validation (included with FastAPI)
# 6. python-dotenv for environment variable management
# 7. httpx for async HTTP calls to external services

fastapi==0.104.1
uvicorn[standard]==0.24.0
plaid-python==16.0.0
pandas==2.1.4
numpy==1.26.2
sqlalchemy==2.0.23
alembic==1.13.0
python-dotenv==1.0.0
httpx==0.25.2
pytest==7.4.3
pytest-asyncio==0.23.2
